import { CSSProperties } from "react";

const styles: { [key: string]: CSSProperties } = {
  header: {
    padding: "20px 0 40px"
  },

  title: {
    textAlign: "center",
    fontSize: 34,
    fontWeight: "bold"
  }
};

export default styles;
